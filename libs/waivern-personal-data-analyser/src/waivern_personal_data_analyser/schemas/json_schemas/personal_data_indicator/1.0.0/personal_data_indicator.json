{
  "$defs": {
    "AnalysisChainEntry": {
      "description": "Single entry in an analysis chain tracking sequence of analysers.",
      "properties": {
        "order": {
          "description": "Sequence number in the analysis chain",
          "minimum": 1,
          "title": "Order",
          "type": "integer"
        },
        "analyser": {
          "description": "Name of the analyser that performed this step",
          "title": "Analyser",
          "type": "string"
        },
        "execution_timestamp": {
          "description": "When this analysis was performed",
          "title": "Execution Timestamp",
          "type": "string"
        }
      },
      "required": [
        "order",
        "analyser"
      ],
      "title": "AnalysisChainEntry",
      "type": "object"
    },
    "BaseAnalysisOutputMetadata": {
      "additionalProperties": true,
      "description": "Base metadata for analysis outputs with chain tracking.",
      "properties": {
        "ruleset_used": {
          "description": "Name of the ruleset used for analysis",
          "title": "Ruleset Used",
          "type": "string"
        },
        "llm_validation_enabled": {
          "description": "Whether LLM validation was enabled for this analysis",
          "title": "Llm Validation Enabled",
          "type": "boolean"
        },
        "analysis_timestamp": {
          "description": "ISO 8601 timestamp when the analysis was performed",
          "title": "Analysis Timestamp",
          "type": "string"
        },
        "evidence_context_size": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Size of context used for evidence extraction",
          "title": "Evidence Context Size"
        },
        "analyses_chain": {
          "description": "Track the full analysis chain",
          "items": {
            "$ref": "#/$defs/AnalysisChainEntry"
          },
          "minItems": 1,
          "title": "Analyses Chain",
          "type": "array"
        }
      },
      "required": [
        "ruleset_used",
        "llm_validation_enabled",
        "analyses_chain"
      ],
      "title": "BaseAnalysisOutputMetadata",
      "type": "object"
    },
    "BaseFindingEvidence": {
      "description": "Evidence item with content and collection timestamp.",
      "properties": {
        "content": {
          "description": "The evidence content snippet",
          "title": "Content",
          "type": "string"
        },
        "collection_timestamp": {
          "description": "When the evidence was collected",
          "title": "Collection Timestamp",
          "type": "string"
        }
      },
      "required": [
        "content"
      ],
      "title": "BaseFindingEvidence",
      "type": "object"
    },
    "PersonalDataIndicatorMetadata": {
      "additionalProperties": false,
      "description": "Metadata for personal data indicator findings.\n\nExtends BaseFindingMetadata which provides:\n- source: str - Source file or location where the data was found\n- context: dict[str, object] - Extensible context for pipeline metadata",
      "properties": {
        "source": {
          "description": "Source file or location where the data was found",
          "title": "Source",
          "type": "string"
        },
        "context": {
          "additionalProperties": true,
          "description": "Extensible context for pipeline metadata (connector_type, artifact_id, etc.)",
          "title": "Context",
          "type": "object"
        }
      },
      "required": [
        "source"
      ],
      "title": "PersonalDataIndicatorMetadata",
      "type": "object"
    },
    "PersonalDataIndicatorModel": {
      "description": "Personal data indicator finding structure.\n\nThis is a framework-agnostic indicator that identifies personal data patterns.\nRegulatory classification (e.g., GDPR special categories) is performed by\ndownstream classifiers.",
      "properties": {
        "evidence": {
          "description": "Evidence items with content and timestamps for this finding",
          "items": {
            "$ref": "#/$defs/BaseFindingEvidence"
          },
          "minItems": 1,
          "title": "Evidence",
          "type": "array"
        },
        "matched_patterns": {
          "description": "Patterns that were matched during analysis",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Matched Patterns",
          "type": "array"
        },
        "type": {
          "description": "Type of personal data indicator (e.g., 'email', 'phone_number')",
          "title": "Type",
          "type": "string"
        },
        "data_type": {
          "description": "Categorical data type identifier (e.g., 'basic_profile', 'health_data')",
          "title": "Data Type",
          "type": "string"
        },
        "metadata": {
          "anyOf": [
            {
              "$ref": "#/$defs/PersonalDataIndicatorMetadata"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Additional metadata from the original data source"
        }
      },
      "required": [
        "evidence",
        "matched_patterns",
        "type",
        "data_type"
      ],
      "title": "PersonalDataIndicatorModel",
      "type": "object"
    },
    "PersonalDataIndicatorSummary": {
      "description": "Summary statistics for personal data indicator findings.",
      "properties": {
        "total_findings": {
          "description": "Total number of personal data indicators found",
          "minimum": 0,
          "title": "Total Findings",
          "type": "integer"
        }
      },
      "required": [
        "total_findings"
      ],
      "title": "PersonalDataIndicatorSummary",
      "type": "object"
    },
    "PersonalDataValidationSummary": {
      "description": "LLM validation summary for personal data findings.",
      "properties": {
        "llm_validation_enabled": {
          "default": true,
          "description": "Whether LLM validation was enabled",
          "title": "Llm Validation Enabled",
          "type": "boolean"
        },
        "original_findings_count": {
          "description": "Number of findings before validation",
          "minimum": 0,
          "title": "Original Findings Count",
          "type": "integer"
        },
        "validated_findings_count": {
          "description": "Number of findings after validation",
          "minimum": 0,
          "title": "Validated Findings Count",
          "type": "integer"
        },
        "false_positives_removed": {
          "description": "Number of false positives removed by validation",
          "minimum": 0,
          "title": "False Positives Removed",
          "type": "integer"
        },
        "validation_mode": {
          "description": "LLM validation mode used",
          "title": "Validation Mode",
          "type": "string"
        }
      },
      "required": [
        "original_findings_count",
        "validated_findings_count",
        "false_positives_removed",
        "validation_mode"
      ],
      "title": "PersonalDataValidationSummary",
      "type": "object"
    }
  },
  "description": "Complete output structure for personal_data_indicator schema.\n\nThis model represents the full wire format for personal data indicator results.\nThese indicators are framework-agnostic and can be consumed by regulatory\nclassifiers (e.g., GDPR, CCPA) for framework-specific enrichment.",
  "properties": {
    "findings": {
      "description": "List of personal data indicators found",
      "items": {
        "$ref": "#/$defs/PersonalDataIndicatorModel"
      },
      "title": "Findings",
      "type": "array"
    },
    "summary": {
      "$ref": "#/$defs/PersonalDataIndicatorSummary",
      "description": "Summary statistics of the personal data analysis"
    },
    "analysis_metadata": {
      "$ref": "#/$defs/BaseAnalysisOutputMetadata",
      "description": "Metadata about the analysis process"
    },
    "validation_summary": {
      "anyOf": [
        {
          "$ref": "#/$defs/PersonalDataValidationSummary"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "LLM validation summary (if validation was applied)"
    }
  },
  "required": [
    "findings",
    "summary",
    "analysis_metadata"
  ],
  "title": "PersonalDataIndicatorOutput",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "version": "1.0.0"
}
